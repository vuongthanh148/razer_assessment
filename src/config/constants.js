export const APP_ENV = {
    PROD: "PROD",
    DEV: "DEV"
}

export const TOKEN_TYPE = {
    ACCESS: 'ACCESS',
    REFRESH: 'REFRESH',
};

export const CATEGORY_ENUM = ["featured", "popular", "game-credits", "gift-cards", "games", "services", "newly-added", "direct-top-up"]

export const GENRE_ENUM = ["others", "action", "adventure", "casual", "mmo", "moba", "rpg", "shooter", "simulation", "social", "sports", "strategy"]

export const PLATFORM_ENUM = ["desktop", "mobile", "console"]